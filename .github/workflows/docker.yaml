name: Docker build fron-end
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: docker build
      run:
        docker build . -t artipfw/front-end:1.0.0
